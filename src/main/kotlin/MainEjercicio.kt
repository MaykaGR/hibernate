import jakarta.persistence.EntityManager
import jakarta.persistence.EntityManagerFactory
import jakarta.persistence.Persistence

//fun main() {
  //  val emf: EntityManagerFactory = Persistence.createEntityManagerFactory("PersistenciaEjercicio")
    //val manager: EntityManager = emf.createEntityManager()
    //val direccion = Direccion("yugday",1,"adad","sda")
    //var taller = Taller("adjhbja","haudig",direccion)
    //manager.transaction.begin()

   /* manager.persist(taller)
    manager.transaction.commit()
    manager.detach(taller)

    val direccion2 = Direccion("uhadu",4,"aygd","ujaygd")
    taller = Taller("uhdiuah","ayugd", direccion2)
    manager.transaction.begin()

    manager.persist(taller)
    manager.transaction.commit()
    manager.detach(taller)

    val direccion3 = Direccion("bdhkajbda",6,"sda","adadasda")
    var cliente = Cliente("adhaujhd","ahdikuah","aytfd",67,direccion3)
    manager.transaction.begin()

    manager.persist(cliente)
    manager.transaction.commit()
    manager.detach(cliente)

    val direccion4 = Direccion("augda",8,"aiuhiu","yaugd")
    cliente = Cliente("iuhda","aiudh","yafd",134,direccion4)
    manager.transaction.begin()

    manager.persist(cliente)
    manager.transaction.commit()
    manager.detach(cliente)*/
//}